<UserControl x:Class="MS2.DesktopApp.Presentation.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->
        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="10">
            <TextBlock Text="BÁO CÁO DOANH THU" 
                       FontSize="18" 
                       FontWeight="Bold"/>
        </Border>

        <!-- DATE FILTER -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="Từ ngày:" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <DatePicker Grid.Column="1" 
                            SelectedDate="{Binding FromDate}"
                            FontSize="14"/>

                <TextBlock Grid.Column="2" 
                           Text="Đến ngày:" 
                           VerticalAlignment="Center"
                           Margin="20,0,10,0"/>

                <DatePicker Grid.Column="3" 
                            SelectedDate="{Binding ToDate}"
                            FontSize="14"/>

                <Button Grid.Column="4" 
                        Content="XEM BÁO CÁO" 
                        Width="120"
                        Height="30"
                        Margin="20,0,0,0"
                        Command="{Binding LoadReportCommand}"/>
            </Grid>
        </Border>

        <!-- REPORT DATA -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- SUMMARY -->
            <Border Grid.Row="0" 
                    BorderBrush="Gray" 
                    BorderThickness="1" 
                    Padding="15"
                    Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="TỔNG DOANH THU" 
                                   FontSize="12" 
                                   Foreground="Gray"/>
                        <TextBlock FontSize="24" 
                                   FontWeight="Bold"
                                   Foreground="Green">
                            <Run Text="{Binding TotalRevenue, StringFormat='{}{0:N0}'}"/>
                            <Run Text="đ"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="TỔNG ĐỚN HÀNG" 
                                   FontSize="12" 
                                   Foreground="Gray"/>
                        <TextBlock FontSize="24" 
                                   FontWeight="Bold"
                                   Foreground="Blue">
                            <Run Text="{Binding TotalOrders}"/>
                            <Run Text=" đơn"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- DETAIL TABLE -->
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Orders}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã đơn" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Ngày" Binding="{Binding OrderDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                    <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding EmployeeName}" Width="150"/>
                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat='{}{0:N0}đ'}" Width="120"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- STATUS BAR -->
        <Border Grid.Row="3" 
                BorderBrush="Gray" 
                BorderThickness="0,1,0,0" 
                Padding="10,5"
                Background="LightGray">
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
