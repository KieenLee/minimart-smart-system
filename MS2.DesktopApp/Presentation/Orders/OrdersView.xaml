<UserControl x:Class="MS2.DesktopApp.Presentation.Orders.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="ĐƠN HÀNG ONLINE (ĐANG CHỜ DUYỆT)" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <DataGrid Grid.Row="1" ItemsSource="{Binding PendingOrders}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" RowHeight="40" FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Đơn" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Khách Hàng" Binding="{Binding CustomerName}" Width="140"/>
                <DataGridTextColumn Header="Ngày Đặt" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TotalAmount, StringFormat=N0}" Width="120"/>
                <DataGridTextColumn Header="Ghi Chú" Binding="{Binding Notes}" Width="*"/>
                <DataGridTemplateColumn Header="Hành Động" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Duyệt Giao Hàng" 
                                    Command="{Binding DataContext.ApproveOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding Id}"
                                    Background="#4CAF50" Foreground="White" Padding="10,5" Cursor="Hand" BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
