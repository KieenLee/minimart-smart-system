<UserControl x:Class="MS2.DesktopApp.Presentation.Employees.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#F1F5F9">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TOPBAR -->
        <Border Grid.Row="0" Background="White" Padding="20,14"
                BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="👥  QUẢN LÝ NHÂN VIÊN"
                           FontSize="20" FontWeight="Bold" Foreground="#1A2340"
                           VerticalAlignment="Center"/>
                <Button Content="↻ Tải lại" Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Right" Height="36"
                        Command="{Binding LoadEmployeesCommand}"/>
            </Grid>
        </Border>

        <!-- SEARCH + CREATE -->
        <Border Grid.Row="1" Background="White" Padding="20,12"
                BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔍" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#94A3B8"/>
                    <TextBox Width="300" Height="36" VerticalContentAlignment="Center"
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,10,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchEmployeesCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Content="Tìm kiếm" Style="{StaticResource PrimaryButton}"
                            Height="36" Command="{Binding SearchEmployeesCommand}"/>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="➕  TẠO NHÂN VIÊN MỚI"
                        Style="{StaticResource SuccessButton}"
                        Height="36" FontWeight="Bold"
                        Click="CreateEmployeeButton_Click"/>
            </Grid>
        </Border>

        <!-- TABLE -->
        <Border Grid.Row="2" Background="White" Margin="16"
                CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.04" BlurRadius="8" ShadowDepth="1"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Employees}"
                      Margin="0" BorderThickness="0" RowHeight="44">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="55"/>
                    <DataGridTextColumn Header="Họ và tên" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="210"/>
                    <DataGridTextColumn Header="Điện thoại" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Vai trò" Binding="{Binding Role}" Width="100"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="65"/>
                    <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy'}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- STATUS BAR -->
        <Border Grid.Row="3" Background="White"
                BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="20,8">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#64748B"/>
        </Border>
    </Grid>
</UserControl>
