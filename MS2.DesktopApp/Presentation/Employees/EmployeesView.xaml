<UserControl x:Class="MS2.DesktopApp.Presentation.Employees.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->
        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="10">
            <Grid>
                <TextBlock Text="QUẢN LÝ NHÂN VIÊN" 
                           FontSize="18" 
                           FontWeight="Bold"/>

                <Button Content="TẢI LẠI" 
                        Width="100"
                        Height="30"
                        HorizontalAlignment="Right"
                        Command="{Binding LoadEmployeesCommand}"/>
            </Grid>
        </Border>

        <!-- SEARCH AND CREATE BAR -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Width="300" 
                             Height="30" 
                             VerticalContentAlignment="Center"
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,10,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchEmployeesCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Content="TÌM KIẾM" 
                            Width="90"
                            Height="30"
                            Command="{Binding SearchEmployeesCommand}"/>
                </StackPanel>

                <!-- Create Button -->
                <Button Grid.Column="1" 
                        Content="➕ TẠO NHÂN VIÊN MỚI" 
                        Width="180"
                        Height="35"
                        FontWeight="Bold"
                        Click="CreateEmployeeButton_Click"/>
            </Grid>
        </Border>

        <!-- EMPLOYEE LIST -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Employees}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Điện thoại" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Vai trò" Binding="{Binding Role}" Width="100"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy'}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- STATUS BAR -->
        <Border Grid.Row="3" 
                BorderBrush="Gray" 
                BorderThickness="0,1,0,0" 
                Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
